<h1>Teams</h1>

<h4><%= link_to "Create a team", new_team_path %></h4>

<% @teams.each do |team| %>
	<%= team.id %> • 
	<%= team.name%> 
	(<%= team.shortname%>) - 
	<%= team.city%>

	(<small><%= link_to "Edit", edit_team_path(team.id) %></small>)

	(<small><%= link_to "Delete", team_path(team), data: { turbo_method: :delete, turbo_confirm: 'Are you sure?' } %></small>)

	<br>
<% end %>